# Personal / machine-specific — regenerated by install-stop-hook. Never commit.
env.sh
agent-hooks.json

# Runtime state lives outside this dir (~/.unlazy/), listed here only as a guard
# in case someone relocates it inside the repo.
.unlazy/
hook-status.log
.stop-heartbeat
.session-start

# Editor / OS / Python cruft
__pycache__/
*.pyc
.DS_Store
